# Non-essential files
# These files are not needed for world building.
icon.png
session.lock

# Player files
# Not actually part of the world project itself.
playerdata
advancements
stats

# Xaero's World Map mod files
# Files generated by this mod.
xaeromap.txt

# The nether and end
# Servers don't load worlds like this anyway, so these files are pointless.
DIM-1
DIM1

# "Old" data files
*.dat_old

# Not important aspects of the world
data
poi

# Datapacks folder
# There are no datapacks in this world.
datapacks

# Entities
# There are no entities on this map - yet. If entities are added (E.g., item frames, this should be unignored).
entities
